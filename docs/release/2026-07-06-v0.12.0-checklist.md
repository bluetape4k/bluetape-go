# v0.12.0 Release Checklist

> 한국어 릴리스 경계: 이 문서는 릴리스 절차를 한국어 독자가 추적할 수 있도록 정리한다. 버전, 태그, 권한 게이트, 명령, CI 증거, GitHub 상태 이름은 원문의 재현성 앵커로 보존한다.

Target version: `v0.12.0`
Target date: 2026-07-06
Release authority: user-requested stable Go module release.
Target branch/tag authority: promote `develop` to `main`, then create the tag on `main`.
Consumer scope: local downstream Go modules that import `github.com/bluetape4k/bluetape-go`.

## 사전 점검 스냅샷

- Local branch at checklist creation: `develop` clean and synced with `origin/develop`.
- Current release: `v0.11.0` exists as a tag and GitHub Release.
- Existing target tag: none for `v0.12.0`.
- Existing target GitHub Release: none for `v0.12.0`.
- Milestone `0.12.0`: open issues 0, closed issues 20, state open.
- Open PRs before release-history work: 0.

## dispatch 보류 증거

- Do not create `v0.12.0` tag until `CHANGELOG.md` contains
  `## [v0.12.0] - 2026-07-06` on the commit being tagged.
- Do not tag before release-history PR CI passes and merges to `develop`.
- Do not close milestone `0.12.0` until the changelog gate is satisfied on
  `develop`.
- Do not promote `develop` to `main` until local `make ci` and GitHub CI pass.
- Do not create GitHub Release until the pushed tag dereferences to `origin/main`.

## 단계 상태

| 단계 | 상태 | Evidence |
| --- | --- | --- |
| Preflight | PASS | `v0.12.0` tag/release absent; open issues 0; open PRs 0. |
| Changelog gate | PASS | Release-history branch adds `## [v0.12.0] - 2026-07-06`. |
| Local validation | PASS | `git diff --check` and `make ci` passed after clearing stale golangci-lint cache. |
| GitHub CI | PENDING | Required for release-history PR and release promotion PR. |
| Milestone close | PENDING | Close after release-history PR merges. |
| Main promotion | PENDING | Release PR from `develop` to `main`. |
| Tag | PENDING | Annotated `v0.12.0` tag on `main`. |
| GitHub Release | PENDING | Notes sourced from `CHANGELOG.md`. |
| Downstream consumers | PENDING | Discover local Go modules after release exists. |
