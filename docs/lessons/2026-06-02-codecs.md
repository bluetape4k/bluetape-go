# Codecs

Issue #11은 codec을 좁은 top-level package로 유지한다. Base64와 Hex는 Go
standard library wrapper를 우선하고, bluetape-go 전용 코드는 Base58/Base62처럼
stdlib에 없는 alphabet에만 둔다. URL62는 별도 format처럼 만들지 말고, 이미
URL-safe인 Base62 alphabet의 문서화된 alias로 취급한다.
