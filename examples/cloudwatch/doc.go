// Package cloudwatchexample contains compile-checked CloudWatch Metrics and
// CloudWatch Logs request examples.
//
// The examples keep AWS configuration, credentials, retries, logging, and
// client lifecycle with the caller. They intentionally use fake clients in
// tests so the default test suite never needs live AWS credentials.
package cloudwatchexample
